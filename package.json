{
  "name": "elisym-eval",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "changeset": "changeset",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint packages/eval/src/ packages/eval-adapter-solana/src/",
    "qa": "turbo run build && turbo run test && turbo run typecheck && bun run lint && oxfmt --check . && bun run spell && turbo run dataset:check",
    "release": "turbo run build && changeset publish",
    "spell": "cspell --no-progress \"packages/eval/src/**\" \"packages/eval/tests/**\" \"packages/eval/datasets/**/*.ts\" \"packages/eval-adapter-solana/src/**\" \"packages/eval-adapter-solana/tests/**\" \"*.md\" \"packages/*/README.md\"",
    "test": "turbo run test",
    "test:live": "turbo run test:live",
    "typecheck": "turbo run typecheck",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "~2.31.0",
    "cspell": "~9.0.0",
    "oxfmt": "~0.42.0",
    "oxlint": "~0.16.0",
    "turbo": "~2.4.0",
    "typescript": "~5.7.0"
  },
  "packageManager": "bun@1.3.11"
}
